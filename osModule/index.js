// Operating system module , we can get info of OS through this module




const os = require("os");

console.log(os.freemem()); // available memory 
console.log(os.homedir()); // in what directory we are in 

console.log(os.machine()); // what machihe we're in i.e x86_64  (64bit)



